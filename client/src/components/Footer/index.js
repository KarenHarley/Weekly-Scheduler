import { Link } from "react-router-dom";

const Footer = ({ data }) => {
  console.log(data);
  return (
    <div className="footer-wrapper">
      This is the footer
    </div>
  );
};

export default Footer;